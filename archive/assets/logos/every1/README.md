# EVERY1, the previous mark set

Retired at v7.2, superseded by the standalone identity. Kept because an audit
citing an earlier version has to be checkable against what that version published,
which is the same reason retired guides live in `archive/`.

| Retired | Superseded by |
| --- | --- |
| `every1-wordmark.svg` | `every1-bare.svg`, the name alone |
| `every1-glyph.svg` | `every1-numeral.svg`, the 1 on its own |
| `every1-app-icon.svg` | `every1-e1.svg`, the E1 icon |
| `every1-vision.svg` | `every1-vision.svg`, redrawn |

Nothing here is published. `archive/` is skipped by the build, so these files are
in no manifest, no pack, and no page.
